apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: diary-ingress
  namespace: diary
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  ingressClassName: nginx
  rules:
    - host: diary.local
      http:
        paths:
          - path: /(.*)
            pathType: Prefix
            backend:
              service:
                name: diary-frontend
                port:
                  number: 80
          - path: /api/?(.*)
            pathType: Prefix
            backend:
              service:
                name: diary-backend
                port:
                  number: 8000
